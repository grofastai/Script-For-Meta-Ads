{
  "name": "@scriptsite/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --port 8000",
    "test": "pytest tests/ -v",
    "lint": "echo no linter configured"
  }
}
